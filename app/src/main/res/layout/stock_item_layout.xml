<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="stockName"
            type="String" />

        <variable
            name="stockTrend"
            type="String" />

        <variable
            name="stockPrice"
            type="Double" />

        <variable
            name="stockOrigin"
            type="String" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:weightSum="10">

            <ImageView
                android:id="@+id/stock_origin_img"
                android:layout_width="@dimen/stock_origin_img_width"
                android:layout_height="@dimen/stock_origin_img_height"
                android:layout_gravity="center_vertical"
                android:layout_marginEnd="8dp"
                android:layout_marginBottom="8dp"
                app:setOriginImage="@{stockOrigin}"
                app:srcCompat="@color/colorPrimary" />

            <TextView
                android:id="@+id/stock_name_text"
                style="@style/stock_item_text_style"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="7"
                app:setText="@{stockName}" />

            <ImageView
                android:id="@+id/stock_trend_img"
                android:layout_width="@dimen/stock_trend_img_width"
                android:layout_height="@dimen/stock_trend_img_height"
                android:layout_gravity="center_vertical"
                android:layout_marginStart="16dp"
                android:layout_marginBottom="8dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:setTrendImage="@{stockTrend}"
                app:srcCompat="@color/colorPrimary" />

            <TextView
                android:id="@+id/stock_price_text"
                style="@style/stock_item_text_style"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="3"
                android:textAlignment="textEnd"
                app:setText="@{stockPrice}" />

        </LinearLayout>

        <View
            android:id="@+id/divider"
            android:layout_width="wrap_content"
            android:layout_height="2dp"
            android:background="@color/lightGray" />

    </LinearLayout>

</layout>